plugins {
    id 'nf-iridanext'
}

iridanext {
    enabled = true
    output {
        path = "${params.outdir}/iridanext.output.json.gz"
        overwrite = true
        files {
            idkey = "run_accession"
            samples = ["**/fastq/*.fastq.gz"]
        }
    
        metadata {
            samples 
                csv {
                    path = "**/samplesheet/samplesheet.csv"
                    idcol = "run_accession"
            }
        }
    }
}
